import React, {PropsWithChildren} from "react";

const Home: React.FC<PropsWithChildren> = ({children}) => {

  return (
    <>
      <main>
      </main>
      {children}
    </>
  )
}

export default Home;
